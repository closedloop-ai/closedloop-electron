# PLN-535 / PRD-258 System Health Check: Plugin Updates

## Inputs

- Repo: `closedloop-electron-prd-258`
- Work item: PRD-258
- Plan sources:
  - ClosedLoop MCP `PLN-535`, latestVersion 3, status APPROVED, id `019e13fe-5c78-730d-bcae-d9ebe1d2529d`
  - [/Users/daniel.ochoa/Source/.goal-plans/find-all-of-my-assigned-features-019c963c-019e0805/PRD-258-implementation-plan.md](/Users/daniel.ochoa/Source/.goal-plans/find-all-of-my-assigned-features-019c963c-019e0805/PRD-258-implementation-plan.md)
- Current code sources:
  - [apps/desktop/src/server/operations/health-check.ts](/Users/daniel.ochoa/Source/closedloop-electron-prd-258/apps/desktop/src/server/operations/health-check.ts:1)
  - [apps/desktop/src/server/operations/plugin-cache.ts](/Users/daniel.ochoa/Source/closedloop-electron-prd-258/apps/desktop/src/server/operations/plugin-cache.ts:1)
  - [apps/desktop/src/main/telemetry-protocol.ts](/Users/daniel.ochoa/Source/closedloop-electron-prd-258/apps/desktop/src/main/telemetry-protocol.ts:1)
  - [apps/desktop/src/main/observability.ts](/Users/daniel.ochoa/Source/closedloop-electron-prd-258/apps/desktop/src/main/observability.ts:1)
  - [apps/desktop/test/gateway-server.test.ts](/Users/daniel.ochoa/Source/closedloop-electron-prd-258/apps/desktop/test/gateway-server.test.ts:4232)
- Repo guardrails:
  - Gateway HTTP route contracts are independently shipped and must remain backward compatible.
  - Optional external payload fields stay omitted when absent, not serialized as `null`.
  - Desktop telemetry diagnostics are consumed by `symphony-alpha`; matching consumer schema/tests are required.
  - Any `apps/desktop/**` change requires one `apps/desktop/package.json` version bump unless already bumped.

## Behavior Areas

- Plugin update opt-in and version-check flow: Desktop runs mutation only on explicit `pluginAutoUpdate=1`, only after install checks pass and manifest comparison proves outdated plugins.
- Update command execution, timeout, suppression, and remediation: CLI execution must be bounded, classifiable, and user-remediable without hanging health check.
- Telemetry and logging contract: Desktop emits exact `diagnostics.pluginUpdate` shape and bounded logs/PostHog properties.
- Backward-compatible response shape: older web clients must keep old behavior and new fields remain additive/omitted when not relevant.

## Behavioral Edge-Case Expansion

- Structured-result contracts: rows E1-E8 cover query parsing, manifest fetch, JSON parsing, command setup, and response shaping.
- Exported boundary invariants: update helper must classify failures itself; health-check route must default disabled even if callers omit or send unknown query values.
- Contract-signal precedence: rows E3-E7 distinguish installed version, latest manifest version, command exit, timeout, reread state, and suppression cache.
- Library-managed lifecycle re-entry: row E7 covers repeated health checks in the same process session.
- Active-processing re-entry: health checks are independent requests; suppression avoids repeated failed attempts but does not queue or lock all checks.
- State propagation across isolation boundaries: rows E4-E6 cover child process output/exit/timeout returning into final row building and telemetry.
- Cross-surface propagation and reconciliation: rows E1/E9 cover Desktop response and telemetry consumed by `symphony-alpha`; no persistent local state changes beyond Claude plugin update itself.
- Data visibility versus side effects: rows E5/E9 separate returned check rows from gateway logs, relay telemetry, and PostHog events.
- Cached capability drift: no peer capability cache; manifest/version and suppression cache covered by E2/E7.
- Terminal-state transition guards: no terminal app state; repeated failed attempts handled by suppression cache.
- Cancellation and shutdown: row E4 covers command timeout, process kill, and health-check response instead of hang.
- Finalizer-visible cleanup state: no new finalizer; child process timeout cleanup is owned by `execFile`/Abort behavior and tested.
- Time-bound credentials/signatures: no new credentials/signatures.
- One-time side effects: Claude plugin update is a mutation; rows E1/E4/E7 gate first attempt, retry suppression, and skipped repeated attempt.
- Durable finalization and replay eligibility: not a job finalization flow.
- Shared durable resources: no explicit deletion/rotation; external Claude plugin state may change only through opted-in CLI.
- Profile or config cloning: not applicable.
- Backward-compatible persisted defaults and promotion: response fields omitted unless update path ran; old web clients ignore additions.
- Distributed lifecycle coverage: rows E1-E9 cover register/no-op, opt-in mutation, repeated request, old caller, telemetry consumer.
- Diagnostic contracts: rows E4/E8/E9 inventory `success`, `failed`, `timeout`, `skipped`, failure reasons, stderr tail, elapsed time, exit code.
- Advertised status inventory: `updateOutcome` values and `plugin_update.*` categories must each be produced by a branch/test.
- Exception scope: manifest/network/JSON/CLI setup failures must not collapse health check into thrown route failure.
- Serverless async side effects: not applicable inside Desktop gateway.
- Path and identity policy canonicalization: query param must be exact literal `1`; plugin refs come from a fixed dataset.
- Transformed input validation parity: absent/empty/non-`1` query values all disabled; plugin refs are not user input.
- Canonical value persistence: no new Desktop persistence; suppression key uses canonical plugin ref plus installed/latest versions.
- Partial update preservation: check rows merge only intended plugin rows and preserve unrelated base checks.
- Persistence access paths: suppression cache is process memory only; no disk schema.
- Side-effect boundary: manifest unavailable, plugins missing, or disabled mode must not run update.
- Testable invariant: Required Tests name positive and one-axis mutations for opt-in, old client, failure, timeout, suppression, and telemetry.

## Shared State Axes

- Request mode: absent, empty/non-`1`, enabled literal `1`.
- Install state: all five ClosedLoop plugins installed, one missing, `bootstrap` absent.
- Version state: installed current, installed outdated, manifest unavailable, invalid installed/latest semver.
- Update process state: success exit, nonzero exit, timeout/killed, CLI unavailable, repeated failed tuple suppressed.
- Reread state: now current, still outdated, partial success, registry unreadable.
- Consumer version: old web no query, new web enabled/disabled, `symphony-alpha` telemetry consumer present.

### Plugin Update Opt-In And Version Flow

#### Current Code

Frozen pre-implementation baseline. Do not rewrite after implementation begins.

| Entry Path | State Inputs | Decision / Branch | Actions / Side Effects | External Outcome | Source |
| --- | --- | --- | --- | --- | --- |
| `GET /api/gateway/health-check` | any query params | Does not inspect `pluginAutoUpdate` | Runs base checks, install checks, and version manifest checks | Outdated plugin rows fail with manual `claude plugin update <plugin>` remediation; no mutation | [health-check.ts](/Users/daniel.ochoa/Source/closedloop-electron-prd-258/apps/desktop/src/server/operations/health-check.ts:67) |
| Plugin install checks | five plugin ids | Requires code, platform, judges, code-review, self-learning | `bootstrap` is not checked | Missing user-installed plugin fails; no bootstrap requirement | [health-check.ts](/Users/daniel.ochoa/Source/closedloop-electron-prd-258/apps/desktop/src/server/operations/health-check.ts:76) |
| `applyPluginVersionChecks` | installed versions and manifest fetch | Fetches latest manifest versions and compares strict semver | No update execution; network/JSON failures become failed rows | Health-check returns rather than throwing on manifest failures | [health-check.ts](/Users/daniel.ochoa/Source/closedloop-electron-prd-258/apps/desktop/src/server/operations/health-check.ts:739) |

#### Intended Change

Frozen target behavior derived from PLN-535. Only change after implementation if recording a plan clarification.

| Entry Path | State Inputs | Decision / Branch | Actions / Side Effects | External Outcome | Source |
| --- | --- | --- | --- | --- | --- |
| E1 Health-check route | absent/empty/non-`1` `pluginAutoUpdate` | Disabled mode | No update command; preserve current checks and omit update metadata | Old web clients keep current behavior | PLN-535 AC-001, compatibility |
| E2 Health-check route | `pluginAutoUpdate=1`, all five installed, one or more outdated | Enabled mode | Run `claude plugin update <plugin>@closedloop-ai --scope user` for outdated plugins, then reread installed versions | Returned plugin rows reflect post-update state | PLN-535 AC-001, AC-002 |
| E3 Install/manifest failure | any plugin missing, manifest fetch rejected, invalid manifest, invalid semver | Do not run update | Return existing install/version failure remediation | No mutation without proof of installed outdated ClosedLoop plugins | PLN-535 risks |
| E4 No `bootstrap` row | `bootstrap` missing | Not part of dataset | No required row, no update attempt for bootstrap | Users without bootstrap do not fail System Check | User-approved revision, PLN-535 AC-013 removed |

### Update Command, Timeout, Suppression, And Remediation

#### Current Code

| Entry Path | State Inputs | Decision / Branch | Actions / Side Effects | External Outcome | Source |
| --- | --- | --- | --- | --- | --- |
| Outdated plugin row | installed older than latest | Marks failed | Remediation is bare command without scope | User must update manually; no metadata or link | [health-check.ts](/Users/daniel.ochoa/Source/closedloop-electron-prd-258/apps/desktop/src/server/operations/health-check.ts:824) |
| Command execution helper | binary checks only | `runCommand` has 3s default timeout and throws on command failures | No dedicated plugin update helper | Cannot classify plugin update timeout/failure separately | [health-check.ts](/Users/daniel.ochoa/Source/closedloop-electron-prd-258/apps/desktop/src/server/operations/health-check.ts:121) |

#### Intended Change

| Entry Path | State Inputs | Decision / Branch | Actions / Side Effects | External Outcome | Source |
| --- | --- | --- | --- | --- | --- |
| E5 Update success | command exits or reread shows current | `updateOutcome: "success"` for affected rows | Passing row includes updated version and `updateAttempted: true` | System Check can pass after auto-update | PLN-535 AC-004 |
| E6 Update failure/timeout | nonzero, timeout, CLI unavailable, still outdated | `failed` or `timeout` outcome | Failing row error explicitly says automatic update was attempted and failed; remediation has steps, scoped command, structured docs link | User gets manual recovery without hung health check | PLN-535 AC-005, AC-006, AC-007 |
| E7 Partial success | multiple outdated plugins, some current after reread | Only still-outdated rows fail; current affected rows succeed | `updatePluginIds` lists affected ids; diagnostics include before/after | Mixed results are visible and actionable | PLN-535 T-1.8 |
| E8 Repeated failed tuple | same plugin ref + installed/latest already failed/timed out this Desktop session | Skip second command | Return current version state with attempted/skipped/failure remediation; no disk persistence | Rechecks do not repeatedly run failing mutation until version tuple changes | PLN-535 T-1.6 |

### Telemetry And Logging Contract

#### Current Code

| Entry Path | State Inputs | Decision / Branch | Actions / Side Effects | External Outcome | Source |
| --- | --- | --- | --- | --- | --- |
| Desktop telemetry types | current categories | No `plugin_update.*` categories or plugin diagnostics | Observability has no helper | Update attempts would not be structured for relay/API | [telemetry-protocol.ts](/Users/daniel.ochoa/Source/closedloop-electron-prd-258/apps/desktop/src/main/telemetry-protocol.ts:12), [observability.ts](/Users/daniel.ochoa/Source/closedloop-electron-prd-258/apps/desktop/src/main/observability.ts:1) |
| Health-check update path | not present | No gateway logs for update attempts | Only healthcheck result telemetry for `claude-cli` allowlist | No before/after/exit/elapsed evidence | [observability.ts](/Users/daniel.ochoa/Source/closedloop-electron-prd-258/apps/desktop/src/main/observability.ts:31) |

#### Intended Change

| Entry Path | State Inputs | Decision / Branch | Actions / Side Effects | External Outcome | Source |
| --- | --- | --- | --- | --- | --- |
| E9 Update batch start/result | affected plugin refs and versions | Emit attempted once, succeeded if all current, failed for nonzero/timeout/still-outdated | Telemetry diagnostics exactly under `diagnostics.pluginUpdate`; PostHog events use bounded props | `symphony-alpha` can parse/query exact shape | PLN-535 AC-011, AC-018 |
| E10 Gateway logs | every attempt and result | Log refs, before/after versions, elapsed, exit code, timeout flag, bounded stderr tail | Do not log auth tokens, env, or full home paths | Support diagnostics without credential exposure | PLN-535 AC-011 |

## Delta Checklist

- Add a fixed user-installed ClosedLoop plugin dataset for `code`, `code-review`, `judges`, `platform`, `self-learning`; do not add `bootstrap`.
- Add optional `CheckResult` update fields and structured `remediationLinks`; omit when no update path ran.
- Add a dedicated plugin update runner with 30s timeout, shell env, resolved Claude path, typed non-throw result, and test hook.
- Refactor version checks to fetch manifest, compare installed/latest, run enabled updates, reread installed versions, and shape final rows.
- Add session-memory suppression keyed by plugin ref + installed/latest version for failed/timeouts.
- Add logging and `diagnostics.pluginUpdate` telemetry helpers/categories/PostHog events.
- Bump `apps/desktop/package.json` exactly once.

## Required Tests

- Health-check route: absent, empty, and non-`1` query values do not run update; literal `1` does.
- Plugin dataset: exactly the five user-installed plugin rows; no required bootstrap row.
- Version/update flow: up-to-date no attempt; outdated success; command failure; timeout; partial success after reread; CLI missing/override invalid; manifest fetch failure non-crashing.
- Suppression: failed tuple skips repeat attempt until installed/latest changes.
- Response metadata: success/failure/timeout/skipped fields emitted only when relevant; old disabled rows omit fields.
- Remediation: failure includes plain-language steps, exact `claude plugin update <plugin>@closedloop-ai --scope user`, and structured docs link.
- Telemetry/logging: exact `diagnostics.pluginUpdate` object for attempted/succeeded/failed; bounded stderr tail and scalar PostHog properties.

## Verification Findings

Pending implementation.

## Fixes Applied

Pending implementation.

## Final Alignment Status

Not aligned

Implementation and validation have not run yet.

## Post-Implementation Verification Findings

| Diagram / Plan Branch | Verification Evidence | Status |
| --- | --- | --- |
| Disabled request preserves current behavior | Health-check query parsing treats only `pluginAutoUpdate=1` as enabled; absent, empty, and other values do not run `claude plugin update` or emit update metadata. | Aligned |
| Enabled request checks installed ClosedLoop plugins | The fixed dataset is exactly `code`, `platform`, `judges`, `code-review`, and `self-learning`; `bootstrap` is not a required user-installed plugin. | Aligned |
| Install/manifest/semver failure branch is non-mutating | Version checks run only after install checks and manifest comparisons succeed; unavailable/invalid inputs keep existing failure rows without update execution. | Aligned |
| Outdated branch runs update and rereads versions | Enabled outdated rows execute `claude plugin update <plugin>@closedloop-ai --scope user`, then reread installed versions before shaping final rows. | Aligned |
| Success/failure/timeout result metadata is additive | Rows include optional `updateAttempted`, `updateOutcome`, `updatePluginIds`, and structured `remediationLinks` only on relevant update paths. | Aligned |
| Repeated failed tuple suppression | Failed/timeout/still-outdated tuples are kept in process memory by plugin ref and version tuple; repeated checks skip rerunning the same failing mutation. | Aligned |
| Telemetry branch emits parseable diagnostics | Desktop emits `plugin_update.attempted`, `plugin_update.succeeded`, and `plugin_update.failed` with bounded `diagnostics.pluginUpdate` payloads matching the symphony-alpha schema. | Aligned |
| Older-client branch remains safe | New Desktop defaults to no mutation unless the query param is exactly `1`, so old web/API clients preserve current health-check behavior. | Aligned |

## Post-Implementation Fixes Applied

- Generated `apps/desktop/src/shared/build-info.ts` via the existing `prebuild` script after `desktop-typecheck` reported the expected missing generated module.
- Verified full Desktop behavior after the generated-file prerequisite rather than changing runtime imports.

## Post-Implementation Validation

- Initial root-level focused command `node --test --import tsx apps/desktop/test/health-check-mcp.test.ts` failed because `tsx` was not resolvable from the repo root; the package-local equivalent below passed.
- `pnpm -C apps/desktop exec tsx --test test/health-check-mcp.test.ts`: passed, 16 tests.
- `just desktop-lint`: passed.
- `just desktop-typecheck`: passed after `pnpm -C apps/desktop run prebuild` generated `build-info.ts`.
- `just desktop-test`: passed, 1341 tests.
- `git diff --check`: passed.

## Post-Implementation Final Alignment Status

Aligned

The final code matches PLN-535 and PRD-258 for the Desktop disabled/enabled request split, exact plugin dataset with no `bootstrap` requirement, update execution/reread/failure suppression, structured remediation, telemetry, compatibility, version bump, and required validation.

## Review Fix Verification Findings - 2026-05-11 Plugin Update Outcome And Telemetry Hardening

| Finding | Decision / Branch | Fix Evidence | Status |
| --- | --- | --- | --- |
| `claude plugin update` could exit 0 while the post-update registry still reported the old plugin version, leaving failed rows with `updateOutcome: "success"` and telemetry outcomes of `success`. | Derive update outcome from the post-update version check. A current final version is `success`; a stale final version is `failed` unless the update was explicitly skipped or timed out. | `resolvePostUpdateOutcome()` is used for both row metadata and `diagnostics.pluginUpdate.outcomes`; `health-check-mcp.test.ts` covers command success plus still-outdated registry returning failed row metadata and failed telemetry with `failureReason: "still_outdated"`. | Aligned |
| Desktop could emit `diagnostics.pluginUpdate` before the consumer preserved and sanitized that payload. | No Electron rollback needed because the companion Symphony consumer already exists at `1f6b50095285dd315d10d38bb9f0a05b3f80f8cf`. | Verified in `/Users/daniel.ochoa/Source/symphony-alpha-prd-258`: schema accepts `diagnostics.pluginUpdate`, handler logs sanitized diagnostics, and tests cover schema preservation plus nested `pluginUpdate.stderrTail` sanitization. | Aligned |
| Plugin update `stderrTail` captured the first 512 characters instead of the suffix. | Keep the bounded suffix for plugin-update command output tails. | `getPluginUpdateOutputTail()` uses `slice(-STDERR_TAIL_MAX_CHARS)`; `health-check-mcp.test.ts` covers preserving the final failure cause. | Aligned |
| PR #172 review thread: auto-update was attempted even when the `claude-cli` health row had already failed. | Gate the mutating auto-update path on both `pluginAutoUpdate=1` and a passing `claude-cli` row. | `shouldEnablePluginAutoUpdate()` is applied before plugin update execution; focused test covers false for failed Claude CLI, true only when requested and Claude CLI passes. | Aligned |
| PR #172 review thread: failed plugin-update telemetry dropped actionable stdout when stderr was empty. | Fall back to a bounded stdout suffix in the existing `stderrTail` diagnostic field when stderr is absent. | `failedOutputTail` uses `stderrTail || bounded stdout tail`; focused test covers stdout-only failure telemetry preserving the suffix and dropping the prefix. | Aligned |

## Review Fixes Applied - 2026-05-11

- Made failed row metadata and plugin-update telemetry outcomes agree with the post-update registry read when an update command exits 0 but does not make the plugin current.
- Changed plugin-update stderr tail handling to preserve the suffix instead of the prefix.
- Confirmed Symphony Alpha already consumes and sanitizes `diagnostics.pluginUpdate`, so no companion checkout change was required.
- Fixed the two active Electron PR #172 threads by gating auto-update on a passing Claude CLI row and falling back to stdout tail for failure diagnostics.

## Review Validation - 2026-05-11

- `pnpm -C apps/desktop exec tsx --test test/health-check-mcp.test.ts`: passed, 20 tests.
- `pnpm -C apps/desktop typecheck`: passed.
- `pnpm -C apps/desktop lint`: passed.
- `git diff --check`: passed.

## Review Final Alignment Status - 2026-05-11

Aligned

The Electron health-check update path now keeps row metadata, telemetry outcomes, diagnostic tails, and mutation gating consistent with PLN-535. No Symphony companion change was needed because the consumer side is already present and tested at the requested commit.
