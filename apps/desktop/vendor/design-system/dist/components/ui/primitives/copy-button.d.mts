import * as React from 'react';

type CopyButtonProps = {
    text: string;
    label?: string;
};
declare function CopyButton({ text, label, }: CopyButtonProps): React.JSX.Element;

export { CopyButton };
